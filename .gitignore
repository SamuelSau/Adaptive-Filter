# Build directories
Debug/
Release/

# Binaries
*.elf
*.bin
*.hex
*.map

# CubeIDE settings (optional)
.settings/
.project
.cproject

# Eclipse/IDE metadata
.metadata/

# Logic analyzer files (used to verify TIM2 interrupts, but not needed in repo)
verified_hardware/
